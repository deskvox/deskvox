SET(NORMTEST_SRC vvmulticasttest.cpp)

# vvMulticasttest
FIND_PACKAGE(Norm)
IF(NORM_FOUND)
  INCLUDE_DIRECTORIES(${NORM_INCLUDE_DIR})
  ADD_EXECUTABLE(normtest ${NORMTEST_SRC})
  TARGET_LINK_LIBRARIES(normtest virvo ${NORM_LIBRARIES} ${PROTOKIT_LIBRARIES})
ENDIF(NORM_FOUND)
