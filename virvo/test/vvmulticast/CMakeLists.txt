SET(MULTICASTTEST_SRC vvmulticasttest.cpp)

# vvMulticasttest
IF(NORM_FOUND)
  INCLUDE_DIRECTORIES(${NORM_INCLUDE_DIRS})
  ADD_EXECUTABLE(multicasttest ${MULTICASTTEST_SRC})
  TARGET_LINK_LIBRARIES(multicasttest virvo ${NORM_LIBRARIES})
ENDIF(NORM_FOUND)
