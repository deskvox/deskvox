SET(NORMTEST_SRC vvmulticasttest.cpp)

# vvMulticasttest
IF(NORM_FOUND)
  INCLUDE_DIRECTORIES(${NORM_INCLUDE_DIRS})
  ADD_EXECUTABLE(normtest ${NORMTEST_SRC})
  TARGET_LINK_LIBRARIES(normtest virvo ${NORM_LIBRARIES})
ENDIF(NORM_FOUND)
