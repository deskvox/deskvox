@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

set(CMAKE_MODULE_PATH_TMP ${CMAKE_MODULE_PATH})
unset(CMAKE_MODULE_PATH)

# virvo dependencies:

# required
find_dependency(GLEW)
find_dependency(OpenGL)
find_dependency(Threads)
find_dependency(Boost COMPONENTS chrono date_time serialization thread OPTIONAL_COMPONENTS system)
# optional
find_package(Cg QUIET)
find_package(FFMPEG QUIET)
find_package(JPEGTURBO QUIET)
find_package(Nifti QUIET)
find_package(NORM QUIET)
find_package(PNG QUIET)
find_package(ZLIB QUIET)
find_package(Protokit QUIET)
find_package(Pthreads QUIET)
find_package(SNAPPY QUIET)
find_package(Teem QUIET)
find_package(VolPack QUIET)

include("${CMAKE_CURRENT_LIST_DIR}/virvo_Exports.cmake")

check_required_components("@PROJECT_NAME@")

set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH_TMP})
